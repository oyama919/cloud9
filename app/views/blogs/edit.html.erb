<%= form_for(@blog) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <%= f.submit 'この内容で修正する' %>
<% end %>

<%= link_to "ブログ一覧画面にもどる", blogs_path %>
